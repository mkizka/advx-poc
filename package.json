{
  "private": true,
  "scripts": {
    "dev:engine": "pnpm dev --filter=@advx/engine",
    "build:engine": "pnpm build --filter=@advx/engine",
    "dev:playground": "pnpm dev --filter=@advx/playground",
    "dev": "pnpm build:engine && run-p dev:engine dev:playground"
  },
  "workspaces": [
    "engine",
    "playground"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
