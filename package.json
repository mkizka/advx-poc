{
  "private": true,
  "scripts": {
    "dev:engine": "yarn workspace @advx/engine dev",
    "build:engine": "yarn workspace @advx/engine build",
    "dev:playground": "yarn workspace @advx/playground dev",
    "dev": "yarn build:engine && run-p dev:engine dev:playground"
  },
  "workspaces": [
    "engine",
    "playground"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
